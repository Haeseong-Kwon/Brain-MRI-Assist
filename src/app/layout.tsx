import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: 'Guardion | AI-Powered Brain MRI Analysis',
  description: 'Advanced neuro-radiology platform featuring AI segmentation, longitudinal tumor tracking, and secure clinical collaboration.',
  keywords: ['MRI', 'Brain Analysis', 'AI Segmentation', 'Neuro-radiology', 'Medical Imaging', 'Tumor Tracking'],
  authors: [{ name: 'Guardion Medical Team' }],
  viewport: 'width=device-width, initial-scale=1, maximum-scale=1',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
